# linux-cron-lab

Simple Bash backup script.

## Files
- `backup-home.sh` - creates a dated tar.gz backup of the user's home directory in `~/backups/`

## How to run
```bash
chmod +x backup-home.sh
./backup-home.sh
